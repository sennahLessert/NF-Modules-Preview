export type Package = {
    title: string,
    headline?: string,
    description?: string,
    items: string[],
    secondBtnText?: string,
    tag?: string,
    hideFooter?: boolean,
    isBigTile?: boolean,
    subsection?: string
}